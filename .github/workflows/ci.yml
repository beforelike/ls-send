# LS send GitHub Actions CI/CD
# 自动打包 Windows EXE 和 Android APK

name: LS Send CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 手动触发

jobs:
  build-windows:
    name: Build Windows EXE
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pyside6
          pip install -r requirements.txt

      - name: Build Windows EXE
        working-directory: ./windows
        run: |
          python build.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: ls-send-windows
          path: dist/LS_send.exe

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Buildozer and dependencies
        run: |
          python -m pip install --upgrade pip
          pip install buildozer plyer kivy websockets
          pip install -r requirements.txt

      - name: Initialize Buildozer
        working-directory: ./android
        run: |
          buildozer android init 2>/dev/null || true

      - name: Build Android APK
        working-directory: ./android
        run: |
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ls-send-android
          path: bin/*.apk

  release:
    name: Create Release
    needs: [build-windows, build-android]
    runs-on: ubuntu-latest
    
    steps:
      - name: Download Windows EXE
        uses: actions/download-artifact@v4
        with:
          name: ls-send-windows
          path: dist/

      - name: Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: ls-send-android
          path: bin/

      - name: Get current date
        id: date
        run: echo "DATE=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.date.outputs.DATE }}
          name: Release ${{ steps.date.outputs.DATE }}
          draft: false
          prerelease: true
          files: |
            dist/LS_send.exe
            bin/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
