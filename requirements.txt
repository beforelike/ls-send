# LS Send Dependencies

# Shared dependencies
# (none - uses only standard library for networking)

# Windows dependencies
PySide6>=6.5.0
pystray>=0.1.9
Pillow>=9.0.0

# Android dependencies (for buildozer.spec)
# kivy>=2.2.0
# kivy-garden
# android-permissions=ACCESS_NETWORK_STATE,INTERNET,WRITE_EXTERNAL_STORAGE,READ_EXTERNAL_STORAGE,FOREGROUND_SERVICE

# Optional: for better performance
# aiohttp>=3.8.0  # Async HTTP (alternative to built-in http.server)
